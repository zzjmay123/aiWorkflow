# 安装指南

## 1. Python 环境准备

### macOS
```bash
# 检查 Python 版本（需要 3.8+）
python3 --version

# 如果未安装，使用 Homebrew 安装
brew install python@3.10
```

### Windows
前往 https://www.python.org/downloads/ 下载 Python 3.10+，安装时勾选 "Add to PATH"

### Linux (Ubuntu/Debian)
```bash
sudo apt update
sudo apt install python3 python3-pip
```

## 2. 安装依赖

```bash
cd ai-workflow-sell
pip install -r requirements.txt

# 安装浏览器（用于截图）
playwright install chromium
```

## 3. 安装小红书 CLI

```bash
# 安装 pipx（Python 包管理工具）
pip install pipx
pipx ensurepath

# 安装小红书官方 CLI
pipx install xiaohongshu-cli

# 登录授权（需扫码）
xiaohongshu auth login
```

**注意**：首次登录需要在小红书 App 内扫码确认，完成账号绑定。

## 4. 验证安装

```bash
# 检查小红书 CLI
xiaohongshu --version

# 测试发布（会生成测试笔记）
cd scripts
python auto_publish_final.py
```

## 5. 图片生成器使用说明

1. 用浏览器打开 `tools/ai-news-generator.html`
2. 修改标题和日期
3. 粘贴生成的文本内容（来自 ai_news_text_only.py）
4. 点击"下载图片"按钮
5. 图片将保存到下载文件夹

## 下一步

安装完成后，请阅读 [配置说明](02-配置说明.md) 进行个性化设置。

---

**常见问题**：
- 安装报错？检查 Python 版本是否 ≥3.8
- playwright 安装失败？尝试 `playwright install-deps`
- 小红书登录失败？确保 App 是最新版本
