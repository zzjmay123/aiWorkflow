# [页面名称] PRD (产品需求文档)

## 1. 引言

### 1.1 背景

> 简要说明该元素/页面的核心定位、解决的问题、用户价值（控制在50字左右）。

```
[需求背景内容]
```

### 1.2 功能清单

> 以表格形式梳理出所有模块、功能点及功能描述。
>
> 功能清单不包含：页面顶栏、左侧菜单栏的样式、交互需求内容！

| 模块             | 功能点         | 功能描述       |
| ---------------- | -------------- | -------------- |
| `[功能模块标题]` | `[功能点标题]` | `[功能点描述]` |

## 2. 页面菜单与权限规划

### 2.1 页面菜单

> 参考菜单栏，描述页面菜单路径。仅描述菜单路径即可！不要做其他内容说明。

**菜单路径**：[一级菜单名称] / [二级菜单名称]

### 2.2 权限规划

> 表格呈现权限点规划，仅定义权限点、可执行操作范围、数据范围（如：个人数据/全量数据），不定义具体角色；
>
> **权限点规划的基本原则：**查看类操作归集在【查看】一个权限点；编辑类操作归集在【编辑】一个权限点；导出类操作归集在【导出】一个权限点。

| 权限点         | 操作范围           | 数据范围      |
| -------------- | ------------------ | ------------- |
| `[权限点名称]` | `[支持的操作名称]` | `[个人/全量]` |

## 3. [模块名称]需求说明

### 3.1 筛选查询区

> 用表格呈现筛选字段、控件类型（如：文本输入/下拉选择/下拉多选/日期范围选择器等）、输入限制（明确长度/枚举值/默认值/必填性/其他条件依赖）、匹配规则（精准/模糊/范围）。

支持多维度组合筛选，筛选条件变更后需点击"查询"按钮生效。

| 筛选字段         | 类型         | 输入限制         | 匹配规则     |
| :--------------- | :----------- | :--------------- | :----------- |
| `[筛选字段名称]` | `[控件类型]` | `[输入限制内容]` | `[匹配规则]` |

- **操作按钮**：

  - **查询**：根据当前筛选条件刷新页面数据。

  - **重置**：恢复所有筛选条件为默认值。

### 3.2 数据列表

> 按页面里列表形式的子模块循环撰写

以列表形式，按需展示详细的数据表格。

- **列表字段**：

  | 列表字段     | 字段说明         | 排序 |
  | :----------- | :--------------- | :--- |
  | `[字段名称]` | `[字段定义描述]` | 是/否 |

- **分页规则**：
  - 每页默认显示：[X]条
  - 可切换显示：[X/X/X]条
  - 分页控件位置：列表底部

- **批量操作**：
  > 描述支持的批量操作，如批量删除、批量导出等

  - **批量删除**：选中多条记录后点击删除，弹出二次确认

- **行操作**：
  > 描述每行支持的操作，如编辑、删除、查看详情等

  - **编辑**：点击编辑按钮，跳转至编辑页面
  - **删除**：点击删除按钮，弹出二次确认

- **交互说明**：
  > 简述数据列表的操作说明，包括导出/排序/行操作说明

  - **排序**：点击表头排序图标，支持升序/降序切换
  - **导出**：点击导出按钮，导出当前筛选条件下的全部数据（CSV格式）

## 4. 交互设计

### 4.1 全局交互

- **加载状态**：数据请求过程中需展示 Loading 状态。
- **空状态**：无数据时需展示统一的 Empty 状态图。
- **响应式**：页面需适配不同分辨率屏幕，保证内容不遮挡、不错位。

### 4.2 特殊交互

> 描述页面中特殊的交互逻辑，如：
> - 筛选条件联动
> - 数据刷新机制
> - 弹窗交互等
