# [页面名称] PRD (产品需求文档)

## 1. 引言

### 1.1 背景

> 简要说明该元素/页面的核心定位、解决的问题、用户价值（控制在50字左右）。

```
[需求背景内容]
```



### 1.2 功能清单

> 以表格形式梳理出所有模块、功能点及功能描述。
>
> 功能清单不包含：页面顶栏、左侧菜单栏的样式、交互需求内容！

| 模块             | 功能点         | 功能描述       |
| ---------------- | -------------- | -------------- |
| `[功能模块标题]` | `[功能点标题]` | `[功能点描述]` |



## 2. 页面菜单与权限规划

### 2.1 页面菜单

> 参考菜单栏，描述页面菜单路径。仅描述菜单路径即可！不要做其他内容说明。

**菜单路径**：[一级菜单名称] / [二级菜单名称]



### 2.2 权限规划

> 表格呈现权限点规划，仅定义权限点、可执行操作范围、数据范围（如：个人数据/全量数据），不定义具体角色；
>
> **权限点规划的基本原则：**查看类操作归集在【查看】一个权限点；编辑类操作归集在【编辑】一个权限点；导出类操作归集在【导出】一个权限点。

| 权限点         | 操作范围           | 数据范围      |
| -------------- | ------------------ | ------------- |
| `[权限点名称]` | `[支持的操作名称]` | `[个人/全量]` |



## 3. [模块名称]需求说明

> 按模块循环撰写



### 3.1 筛选查询区

> 用表格呈现筛选字段、控件类型（如：文本输入/下拉选择/下拉多选/日期范围选择器等）、输入限制（明确长度/枚举值/默认值/必填性/其他条件依赖）、匹配规则（精准/模糊/范围）。

支持多维度组合筛选，筛选条件变更后需点击“查询”按钮生效。

| 筛选字段         | 类型         | 输入限制         | 匹配规则     |
| :--------------- | :----------- | :--------------- | :----------- |
| `[筛选字段名称]` | `[控件类型]` | `[输入限制内容]` | `[匹配规则]` |

- **操作按钮**：

  - **查询**：根据当前筛选条件刷新页面数据。

  - **重置**：恢复所有筛选条件为默认值。

    

### 3.2 [子模块标题]卡片说明

> 按页面里卡片形式**的子模块循环撰写，请严格按照一个子模块一个说明的要求进行撰写，严禁对同类型模块进行合并描述！！！

- **指标列表**：

  > 用表格呈现卡片里展示业务指标的业务价值、指标定义

  | 指标名称     | 业务价值         | 指标定义                            |
  | :----------- | :--------------- | :---------------------------------- |
  | `[指标名称]` | `[指标业务价值]` | `[指标具体的定义（取数逻辑/范围）]` |



- **交互说明**：

  > 简述指标卡片模块的交互说明



### 3.3 [子模块名称]图表说明

> 按页面里**图表形式**的每个子模块独立循环撰写，请严格按照一个子模块一个说明的要求进行撰写，不要对同类型模块进行合并描述！！！
>
> 请识别模块是否包含多个部分，如：即包含图表部分、又包含指标卡片部分，须对每个部分分别进行描述！！！不可遗漏某个部分。

- **[图表名称]说明**：

  > 描述图表模型的图表内容

  - **图表描述**：以`[图标形式]`展示`[图片内容描述]`

  - **图表指标列表**：

    > 用表格呈现图表里展示所有业务指标的业务价值、指标定义，不要用概括性的语言。

    | 指标名称     | 业务价值         | 指标定义                            |
    | :----------- | :--------------- | :---------------------------------- |
    | `[指标名称]` | `[指标业务价值]` | `[指标具体的定义（取数逻辑/范围）]` |

  - **交互说明：**

    > 简述数据列表的操作说明，包括导出/排序/行操作说明

- **[指标卡片名称]说明**：

  > 描述图表模块的指标卡片内容，梳理出所有指标清单，不要进行概括性总结，不要对指标有遗漏！！

  - **卡片指标列表**：

    | 指标名称     | 业务价值         | 指标定义                            |
    | :----------- | :--------------- | :---------------------------------- |
    | `[指标名称]` | `[指标业务价值]` | `[指标具体的定义（取数逻辑/范围）]` |

  - **交互说明**：

    > 简述数据列表的操作说明，包括导出/排序/行操作说明

- **[数据列表名称]说明**：

  > 描述图表模块的数据列表内容，梳理出所有列表字段清单，不要进行概括性总结，不要对字段有遗漏！！

  - **列表字段**：

    | 列表字段     | 字段说明         |
    | :----------- | :--------------- |
    | `[字段名称]` | `[字段定义描述]` |

  - **交互说明**：

    > 简述数据列表的操作说明，包括导出/排序/行操作说明



### 3.4 [子模块名称]数据列表

> 按页面里列表形式**的子模块循环撰写

以列表形式，按需展示详细的数据表格。

- **列表字段**：

  | 列表字段     | 字段说明         |
  | :----------- | :--------------- |
  | `[字段名称]` | `[字段定义描述]` |

  

- **交互说明**：

  > 简述数据列表的操作说明，包括导出/排序/行操作



## 4. 指标总览

> 以表格的形式，梳理出整个数据看板的全量指标清单（**包含所有子模块的所有指标, 包括卡片、图表、数据列表**），描述指标名称、指标筛选维度（日期/场景/终端/用户身份/等）、数据来源（埋点/业务数据）、指标定义（取数逻辑/范围）。
>
> **重点**：一定要全面梳理完整整个数据看板，数据研发需要加工的所有指标内容，该部分内容将作为数据研发的需求清单。整理出来的指标总览需要满足数据看板的全部需求！不可以有遗漏！不可以有遗漏！不可以有遗漏！重点关注：
>
> - 提取每个页面的每个子模块中的指标；
> - 提取指标的范围包含：指标卡片、图表（饼图/折线图/漏斗/等）、数据列表的等
>
> 完成梳理后务必对比所有数据看板模块，仔细检查每个指标是否都已经囊括？如果没有囊括，请进行补充！！！确保没有遗漏！！！

| 指标名称     | 筛选维度                     | 数据来源         | 指标定义           |
| ------------ | ---------------------------- | ---------------- | ------------------ |
| `[指标名称]` | `[筛选维度，用无序列表展示]` | `[指标数据来源]` | `[指标的具体定义]` |



## 5. 交互设计

### 5.1 全局交互

- **加载状态**：数据请求过程中需展示 Loading 状态。
- **空状态**：无数据时需展示统一的 Empty 状态图。
- **响应式**：页面需适配不同分辨率屏幕，保证内容不遮挡、不错位。